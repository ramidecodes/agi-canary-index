# =============================================================================
# AGI Canary Watcher - Environment Variables
# Copy to .env and fill in values. Never commit .env
# =============================================================================

# -----------------------------------------------------------------------------
# Vercel (App) - Next.js, API routes, AI signal processing
# -----------------------------------------------------------------------------
DATABASE_URL=
OPENROUTER_API_KEY=

# -----------------------------------------------------------------------------
# R2 (App document fetch) - For displaying document content in Signal Explorer
# Vercel app needs read access via S3-compatible API (Workers use bindings)
# Create API token in Cloudflare R2 dashboard with Object Read permission
# -----------------------------------------------------------------------------
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=
R2_ENDPOINT=

# -----------------------------------------------------------------------------
# Pipeline Worker (for manual acquisition trigger from admin API)
# -----------------------------------------------------------------------------
# ACQUISITION_WORKER_URL=https://agi-canary-pipeline-dev.xxx.workers.dev
# DISCOVERY_TRIGGER_TOKEN=  # Optional; if set, Vercel sends Bearer token when calling Worker; set same value as Worker secret for /discover and /acquire

# -----------------------------------------------------------------------------
# Optional - App URL for absolute links, OG images, sitemaps
# -----------------------------------------------------------------------------
# NEXT_PUBLIC_APP_URL=https://your-domain.vercel.app

# -----------------------------------------------------------------------------
# Clerk (Auth) - Admin UI and /api/admin require sign-in
# Get keys from https://dashboard.clerk.com
# -----------------------------------------------------------------------------
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
# Optional: in-app sign-in/sign-up and redirect after auth
# NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
# NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
# NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/admin/sources
# NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/admin/sources

# -----------------------------------------------------------------------------
# Cloudflare (Pipeline) - Set via: wrangler secret put <NAME>
# OPENROUTER_API_KEY   (Perplexity + Grok + extraction)
# FIRECRAWL_API_KEY
# -----------------------------------------------------------------------------