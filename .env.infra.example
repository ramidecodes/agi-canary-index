# =============================================================================
# AGI Canary Watcher - Infrastructure Provisioning
# Copy to .env.infra and fill in values. Never commit .env.infra
# Used by infra scripts to provision Cloudflare resources (Workers, R2, Hyperdrive)
# =============================================================================

# -----------------------------------------------------------------------------
# Neon - For Hyperdrive (Cloudflare Workers database connection)
# From Neon dashboard: Connection Details, UNCHECK "Pooled connection"
# Format: postgres://USER:PASSWORD@HOST/database?sslmode=require
# -----------------------------------------------------------------------------
NEON_CONNECTION_STRING=

# -----------------------------------------------------------------------------
# Cloudflare - For CI/CD or scripted provisioning (optional)
# When using wrangler in CI, set these instead of interactive wrangler login
# -----------------------------------------------------------------------------
# CLOUDFLARE_ACCOUNT_ID=
# CLOUDFLARE_API_TOKEN=

# -----------------------------------------------------------------------------
# Pipeline API Keys - Used by infra:secrets to populate wrangler secrets
# Alternatively run: wrangler secret put <NAME> manually
# -----------------------------------------------------------------------------
# OPENROUTER_API_KEY=   # Perplexity (web) + Grok (X) via OpenRouter
# FIRECRAWL_API_KEY=
