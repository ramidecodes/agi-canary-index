"use client";

/**
 * Status badges (Updated, N sources) are no longer shown in the header.
 * On the home page they are rendered in the canary strip at the bottom
 * (see HomePageClient + CanaryStrip status prop).
 */
export function HomeStatusBadges() {
  return null;
}
